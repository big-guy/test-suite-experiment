/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package test.suite.experiment.app;

import test.suite.experiment.list.LinkedList;

import static test.suite.experiment.utilities.StringUtils.join;
import static test.suite.experiment.utilities.StringUtils.split;
import static test.suite.experiment.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
